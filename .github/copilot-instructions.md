# Monthly Bills App

- This workspace contains a React + TypeScript Vite app for tracking monthly bills.
- Keep bill data persistence in `localStorage`.
- Use `crypto.randomUUID()` for new bill IDs.
- Favor small, focused UI updates that preserve the responsive dashboard layout.
- Avoid adding new dependencies unless they clearly simplify the bill-tracking workflow.